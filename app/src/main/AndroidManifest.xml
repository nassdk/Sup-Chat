<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.firabasefirstexperience">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".kotlin.activity.ResetPassActivity"
            android:parentActivityName=".kotlin.activity.LoginActivity">
        </activity>

        <activity android:name=".kotlin.activity.StartLoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".java.activity.SearchActivity" />

        <activity
            android:name=".kotlin.activity.DiffProfileActivity"
            android:parentActivityName=".java.activity.ChatActivity" />

        <activity
            android:name=".kotlin.activity.SearchActivity"
            android:parentActivityName=".java.activity.ChatActivity" />

        <activity
            android:name=".kotlin.activity.ChatActivity"
            android:parentActivityName=".kotlin.activity.MainActivity" />

        <activity
            android:name=".kotlin.activity.LoginActivity"
            android:parentActivityName=".kotlin.activity.StartActivity" />

        <activity
            android:name=".kotlin.activity.MainActivity"
            android:label="Sup Chat"
            android:parentActivityName=".kotlin.activity.StartActivity" />

        <activity
            android:name=".kotlin.activity.RegisterActivity"
            android:parentActivityName=".kotlin.activity.StartActivity" />

        <activity android:name=".kotlin.activity.StartActivity" />
        <activity android:name=".kotlin.activity.ProfileActivity"/>
    </application>

</manifest>