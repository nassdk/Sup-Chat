<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.firabasefirstexperience">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/app_icon"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity android:name=".kotlin.presentation.resetpassword.ui.ResetPassActivity"
            android:parentActivityName=".kotlin.presentation.login.ui.LoginActivity">
        </activity>

        <activity android:name=".kotlin.presentation.StartLoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".kotlin.presentation.diffprofile.ui.DiffProfileActivity"
            android:parentActivityName=".kotlin.presentation.chat.ui.ChatActivity" />

        <activity
            android:name=".kotlin.presentation.searchusers.ui.SearchActivity"
            android:parentActivityName=".kotlin.presentation.chat.ui.ChatActivity" />

        <activity
            android:name=".kotlin.presentation.chat.ui.ChatActivity"
            android:parentActivityName=".kotlin.presentation.main.ui.MainActivity" />

        <activity
            android:name=".kotlin.presentation.login.ui.LoginActivity"
            android:parentActivityName=".kotlin.presentation.startscreen.ui.StartActivity" />

        <activity
            android:name=".kotlin.presentation.main.ui.MainActivity"
            android:label="Sup Chat"/>

        <activity
            android:name=".kotlin.presentation.registerscreen.ui.RegisterActivity"
            android:parentActivityName=".kotlin.presentation.startscreen.ui.StartActivity" />

        <activity android:name=".kotlin.presentation.startscreen.ui.StartActivity" />
        <activity android:name=".kotlin.presentation.profilescreen.ui.ProfileActivity"/>
    </application>

</manifest>